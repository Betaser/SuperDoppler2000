<%= button_to "Subpage", subpage1_path, method: :get %>
<%= button_to "Settings", settings_path, method: :get%>

<h2 hidden="true">Find me in app/view/index/index.html.erb</h2>

<%# data should be a hash or array %>

<%= turbo_stream_from "chart_channel" %>

<div id="main_chart">
	<%= render partial: "subpage1/chart_content", locals: { data: @data, width: @width } %>
</div>